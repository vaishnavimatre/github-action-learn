name: Learn basics of GitHub Actions

on:
  push:
    branches:
      - main

jobs:
  helloworld:
    runs-on: ubuntu-latest

    steps:
      - name: print hello
        run: echo "hello world !!"

      - name: print chalo
        run: echo "chalo world !!"

  chaloworld:
    runs-on: ubuntu-latest

    steps:
      - name: print hello
        run: echo "hello world !!"

      - name: print chalo
        run: echo "chalo world !!"
